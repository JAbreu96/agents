pytest
requests
beautifulsoup4
gspread
google-auth
google-api-python-client
python-dotenv
anthropic
flask
pypdf
reportlab

# MCP SDK, used by mcp_servers/job_tracker/server.py. It was missing from this
# file entirely -- the server ran locally only because the SDK happened to be
# installed in the interpreter. A fresh clone (a cloud routine, or a new laptop)
# could not start the server at all.
#
# Capped below 2.0: the server imports mcp.server.fastmcp, which does not exist
# in v2. An unpinned install resolved to 2.0.0 in a cloud container and failed
# with ModuleNotFoundError after a perfectly successful pip install.
mcp>=1.27.1,<2

# Remote database driver for the cloud pipeline. Only imported when
# TURSO_DATABASE_URL is set, so local development does not need it.
# Pinned exactly: the package ships no __version__ and the ecosystem has
# three competing clients with different sync models.
libsql==0.1.11
